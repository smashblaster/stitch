#pragma once

enum class Inputs {
	A, B, X, Y, Z, L, R, START,
	DDOWN, DLEFT, DRIGHT, DUP,
	XAXIS, YAXIS, LEFT, RIGHT, CXAXIS, CYAXIS,
	length
};

// enum class Buttons {
// 	A, B, X, Y, Z, L, R, START,
// 	DDOWN, DLEFT, DRIGHT, DUP,
// 	length
// };

// enum class Analogs {
// 	XAXIS, YAXIS, LEFT, RIGHT, CXAXIS, CYAXIS,
// 	length
// };
